{
  "name": "connexions",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun bun src/index.html",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "bunx --bun @biomejs/biome lint --write --unsafe ./src",
    "unsafe-lint": "bunx --bun @biomejs/biome lint --write --unsafe ./src"
  },
  "dependencies": {
    "dexie": "^4.3.0",
    "dexie-cloud-addon": "^4.3.9",
    "lit": "^3.3.2"
  },
  "devDependencies": {
    "typescript": "~5.9.3"
  }
}
