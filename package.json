{
  "name": "connexions",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun src/index.html",
    "archive": "bun src/archive.ts",
    "build": "tsc && vite build",
    "preview": "wrangler dev",
    "lint": "bunx @biomejs/biome lint --write ./src",
    "unsafe-lint": "bunx @biomejs/biome lint --write --unsafe ./src",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "dexie": "^4.3.0",
    "dexie-cloud-addon": "^4.4.11",
    "dexie-export-import": "^4.4.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.15",
    "@types/bun": "^1.3.14",
    "fake-indexeddb": "^6.2.5",
    "typescript": "~5.9.3",
    "wrangler": "^4.95.0"
  }
}
